import { StyleSheet } from "react-native";

const orders = StyleSheet.create({
    filters:{
        backgroundColor:'red',
        borderRadius:20,
        borderWidth:1,
        paddingLeft:15,
        paddingRight:15,
        paddingTop:10,
        paddingBottom:10
    },
    filtercontainer:{
        gap:10
    }
})

export {
    orders
}